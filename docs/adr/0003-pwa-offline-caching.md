# PWA for offline caching only

Built Decks are progressively enhanced as PWAs, but only for offline caching via a service worker. Install-to-homescreen and push notifications are out of scope — the goal is that a built Deck works fully offline when opened via a local server or file share, with no internet dependency.
