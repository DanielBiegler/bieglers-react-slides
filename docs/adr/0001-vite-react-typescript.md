# Vite + React + TypeScript as the build stack

We chose Vite (v8.0.16) with React and TypeScript as the build stack for `@danielbiegler/react-slides`. Vite provides a fast dev server with hot reload and produces the static folder output the project requires. React and TypeScript are the authoring surface — slides are TSX components, type safety is a first-class goal.
